<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">  

<mapper namespace="co.kr.shopping.dao.MemberMapper">
	<insert id="insertMember" parameterType="map">
		INSERT INTO member
		(user_Id, user_Pw, user_regDate, user_name, user_gen, user_chDate)
		VALUES 
		(#{mem_id}, #{mem_pw}, now(), #{mem_name}, #{mem_gen}, now())
	</insert>
	
	
</mapper>